<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>

    <settings>
        <setting name="callSettersOnNulls" value="true"></setting>
    </settings>

    <typeAliases>
        <!-- 给单个实体配置别名 -->
        <!-- <typeAlias type="com.soft863.entity.User" alias="user" /> -->
        <!-- 通用配置：entity包下的所有实体类别名为类型首字母变小写 -->
        <package name="com.soft863.entity"></package>
    </typeAliases>

</configuration>